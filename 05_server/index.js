/* este sera nuestro archivo principal del cual arrancaremos el servidor HTTP */

var server = require("./server"); //llamamos a nuestro modulo server y lo ponemos en una variable "x"

server.iniciar(); // ocupamos la funcion iniciar de nuestro modulo server